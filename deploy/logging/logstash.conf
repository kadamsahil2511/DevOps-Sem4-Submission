input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  mutate {
    add_field => { "service" => "tradenet-api" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "tradenet-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
