apiVersion: v1
kind: Service
metadata:
  name: tradenet-api
  namespace: tradenet-apps
spec:
  selector:
    app.kubernetes.io/name: declaration-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
